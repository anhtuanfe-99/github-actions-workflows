name: Node Demo

on:
  push

jobs:
  inspect-node:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: node --version

      - run: npm --version