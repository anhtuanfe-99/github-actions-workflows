name: My First Workflow

on:
  push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello Github Actions!"
      - run: pwd
      - run: ls
      - run: echo "Workflow completed/finished"